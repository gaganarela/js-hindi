const str="RAM"
console.log(`${str} is a good boy`)//string interpolation using backticks
const gamename=new String('shyam')//object datatype now.....so different methods can be called on strings now
console.log(gamename.length)
